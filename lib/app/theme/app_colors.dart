import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color background = Color(0xFF0E0E0E);
  static const Color backgroundDark = Color(0xFF000000);

  // Surface / Cards
  static const Color surface = Color(0xFF1A1A1A);
  static const Color card = Color(0xFF202020);
  static const Color cardLight = Color(0xFF2A2A2A);
  static const Color cardHover = Color(0xFF303030);

  // Nav bar
  static const Color navBar = Color(0xFF1A1A1A);
  static const double navBarOpacity = 0.90;

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);
  static const Color textHint = Color(0xFF666666);
  static const Color textDisabled = Color(0xFF444444);

  // Accent — pure white for glow effects
  static const Color accent = Color(0xFFFFFFFF);

  // Border
  static const Color border = Color(0xFF3A3A3A);
  static const Color divider = Color(0xFF2A2A2A);

  // Star rating
  static const Color starFilled = Color(0xFFFFFFFF);
  static const Color starEmpty = Color(0xFF444444);

  // Utility
  static const Color transparent = Colors.transparent;

  // Category pill active
  static const Color pillActive = Color(0xFFFFFFFF);
  static const Color pillActiveText = Color(0xFF0E0E0E);
  static const Color pillInactive = Color(0xFF2A2A2A);
}
